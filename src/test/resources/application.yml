server:
  servlet:
    context-path: /minesweeper
  port: 8080

spring:
  application:
    name: minesweeper

  jackson:
    property-naming-strategy: SNAKE_CASE
    deserialization:
      fail-on-unknown-properties: false